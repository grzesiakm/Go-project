<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
          integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Web Scrapper - Cheapest Flights</title>
</head>
<body>
<h1>Web Scrapper - Cheapest Flights</h1>

<form action="/search" method="POST">

    <form>
        <div class="row">
            <div class="col">
                <input type="text" class="form-control" name="departure" id="from" placeholder="Departure">
            </div>
            <div class="col">
                <input type="text" class="form-control" name="arrival" id="to" placeholder="Arrival">
            </div>
            <div class="col">
                <input type="text" class="form-control" name="departureTime" id="fromDate"
                       placeholder="Departure Time (YYYY-MM-DD)">
            </div>
            <div class="col">
                <input type="text" class="form-control" name="arrivalTime" id="toDate"
                       placeholder="Arrival Time (YYYY-MM-DD)">
            </div>
        </div>
        <div class="row">
            {{/*    </form>*/}}
            {{/*    <div class="form-group">*/}}
            {{/*    <label for="from">From</label>*/}}
            {{/*    <input type="text" class="form-control" name="departure" id="from" placeholder="Departure">*/}}
            {{/*    </div>*/}}

            {{/*    <div class="form-group">*/}}
            {{/*    <label for="to">To</label>*/}}
            {{/*    <input type="text" class="form-control" name="arrival" id="to" placeholder="Arrival">*/}}
            {{/*    </div>*/}}

            {{/*    <div class="form-group">*/}}
            {{/*    <label for="fromDate">Date from (YYYY-MM-DD)</label>*/}}
            {{/*    <input type="text" class="form-control" name="departureTime" id="fromDate" placeholder="Departure Time">*/}}
            {{/*    </div>*/}}

            {{/*    <div class="form-group">*/}}
            {{/*    <label for="toDate">Date to (YYYY-MM-DD)</label>*/}}
            {{/*    <input type="text" class="form-control" name="arrivalTime" id="toDate" placeholder="Arrival Time">*/}}
            {{/*    </div>*/}}

            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
    </form>

</body>
</html>
